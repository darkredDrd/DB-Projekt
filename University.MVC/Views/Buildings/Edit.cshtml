@model Cinema.MVC.ViewModels.Buildings.BuildingUpdateViewModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Update a building</h2>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Address" class="control-label"></label>
                <input asp-for="Address" class="form-control" />
                <span asp-validation-for="Address" class="text-danger"></span>
            </div>
            
            @* <div class="form-group"> *@
            @*     <label asp-for="StudentCheckboxes" class="control-label">Students:</label> *@
            @*     @for (int i = 0; i < Model.StudentCheckboxes.Count; i++) *@
            @*     { *@
            @*         <div> *@
            @*             <input type="checkbox" asp-for="StudentCheckboxes[i].Checked" /> *@
            @*             <label>@Model.StudentCheckboxes[i].Label</label> *@
            @*             <input type="hidden" asp-for="StudentCheckboxes[i].Id" /> *@
            @*         </div> *@
            @*     } *@
            @* </div> *@
            @* <div class="form-group"> *@
            @*     <label asp-for="TeacherCheckboxes" class="control-label">Teachers:</label> *@
            @*     @for (int i = 0; i < Model.TeacherCheckboxes.Count; i++) *@
            @*     { *@
            @*         <div> *@
            @*             <input type="checkbox" asp-for="TeacherCheckboxes[i].Checked" /> *@
            @*             <label>@Model.TeacherCheckboxes[i].Label</label> *@
            @*             <input type="hidden" asp-for="TeacherCheckboxes[i].Id" /> *@
            @*         </div> *@
            @*     } *@
            @* </div> *@
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
